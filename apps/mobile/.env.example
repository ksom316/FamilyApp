# Public API origin only; never put database credentials or auth secrets here.
# On Web, keep the loopback hostname aligned with the Expo page so browser cookies remain same-site.
EXPO_PUBLIC_API_URL=http://localhost:8787
