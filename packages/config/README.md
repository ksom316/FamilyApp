# Shared configuration

Contains the lightweight semantic design tokens used by future FamilyApp surfaces. Tokens are grouped by light and dark themes so consumers can select a theme without changing component APIs.
